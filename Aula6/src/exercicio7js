// Exercício 7: Escreva um algoritmo para ler as dimensões de um quadrado (lado), calcular e escrever a área do quadrado.

alert("Área do quadrado");

let lado = parseFloat(prompt("Informe o primeiro valor:"));

let resultado = (lado * lado);

alert("O valor da área do quadrado é " + resultado);